<UserControl x:Class="UMLDiagrams.View.UseCaseView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UMLDiagrams.View"
      xmlns:viewModel="clr-namespace:UMLDiagrams.ViewModel"
      xmlns:s="clr-namespace:UMLDiagrams"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance viewModel:EllipseViewModel}">

    <UserControl.Resources>

        <ControlTemplate x:Key="MoveTemplate" TargetType="{x:Type s:MoveThumb}">
            <Rectangle Fill="Transparent"/>
        </ControlTemplate>

        <ControlTemplate x:Key="DesignerItemTemplate" TargetType="ContentControl">
            <Grid DataContext="{Binding RelativeSource ={RelativeSource TemplatedParent}}">
                <s:MoveThumb Template="{StaticResource MoveTemplate}" Cursor="SizeAll"/>
                <ContentPresenter Content="{TemplateBinding ContentControl.Content}"/>
            </Grid>
        </ControlTemplate>

    </UserControl.Resources>


    <Grid>
        
            <ContentControl Template ="{StaticResource DesignerItemTemplate}"/>
        <ItemsControl ItemsSource="{Binding Path=UseCases}"/>
        
    </Grid>
</UserControl>
